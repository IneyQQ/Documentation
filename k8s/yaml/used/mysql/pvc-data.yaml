apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-dev-data
  namespace: mysql
  labels:
    app: tconstructor
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
